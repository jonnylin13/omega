

export class ImgMapleClass {
    data_length: number;
    offset: number;

    constructor(data_length: number, offset: number) {
        this.data_length = data_length;
        this.offset = offset;
    }
}